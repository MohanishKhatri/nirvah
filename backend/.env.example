# sqlite works with zero setup; swap in postgres+pgvector when the container is up
DATABASE_URL=sqlite+aiosqlite:///./nirvah.db
# DATABASE_URL=postgresql+asyncpg://nirvah:nirvah@localhost:5432/nirvah

GEMINI_API_KEY=
RESEND_API_KEY=
RESEND_FROM=NIRVAH <onboarding@resend.dev>

FRONTEND_URL=http://localhost:3000
ADMIN_PASSWORD=nirvah_admin_2024
GOOGLE_CLIENT_ID=
ALLOWED_EMAIL_DOMAIN=nitk.edu.in
REMINDER_AFTER_HOURS=24

# development switches
USE_LLM_MOCK=true          # true until a GEMINI_API_KEY exists
DEV_AUTH_BYPASS=true       # accept any bearer token as demo.student@<domain>
UPLOAD_DIR=./uploads
